# Device integration

The public integration guide now separates the canonical [C Device](./device)
path from the optional [C++ facade](./cpp). Both use the same
[transport and scheduling contract](./transport).
